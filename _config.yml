name: Vishnu's Personal Website
markdown: kramdown
kramdown:
   math_engine: mathjax
defaults:
  - scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
